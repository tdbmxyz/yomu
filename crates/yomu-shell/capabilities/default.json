{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs + OS notifications",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default"]
}
